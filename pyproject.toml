#This file is used to package the PartTotalPrice plugin.
#
#- It was generated by the InvenTree Plugin Creator tool - version 1.13.3
#- Ref: https://github.com/inventree/plugin_creator

[build-system]
requires = ["setuptools", "twine", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "inventree-bellesoft-extra-part-details"
description = "Shows extra part details for Bellesoft AMS."
dynamic = ["version"]
authors = [
    { name = "Bellesoft Systems Inc." }
]
readme = "README.md"
license = "MIT"
keywords = ["inventree", "plugin"]
requires-python = ">=3.9"
dependencies = [
    # Enter your plugin library dependencies here
]
classifiers = [
    "Programming Language :: Python :: 3",
    "Operating System :: OS Independent",
    "Framework :: InvenTree",
]

[project.entry-points."inventree_plugins"]
BellesoftExtraPartDetails = "bellesoft_extra_part_details.core:BellesoftExtraPartDetails"

[tool.setuptools.packages.find]
where = [""]

[tool.setuptools.dynamic]
version = {attr = "bellesoft_extra_part_details.PLUGIN_VERSION"} 

[tool.ruff]
exclude = [
    ".git",
    "__pycache__",
    "test.py",
    "tests",
    "venv",
    "env",
    ".venv",
    ".env",
]

src=["bellesoft_extra_part_details"]

